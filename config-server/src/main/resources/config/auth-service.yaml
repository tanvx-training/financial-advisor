server:
  port: 9000
spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/auth-database
    username: postgres
    password: password
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: none